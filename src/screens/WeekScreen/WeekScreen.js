import React from 'react';
import { Text } from 'react-native';
import { Appbar, IconButton } from 'react-native-paper'

const WeekScreen = ({ navigation }) => (
    <Text>This is WeekScreen.</Text>
)

export default WeekScreen;